# Rust build artifacts
/target/
**/target/
**/*.rs.bk

# wasm-pack / wasm-bindgen output — regenerated by `wasm-pack build` and in CI.
**/pkg/

# Committed Facet wasm fixtures (crates/tessera-ascii/tests/*.wasm and
# packages/facet-abi/test/fixtures/*.wasm) are intentionally TRACKED: they are
# hermetic conformance-test inputs, not build output, and live outside target/
# and pkg/, so nothing above ignores them.

# Node / TypeScript
node_modules/
.next/
out/
dist/
build/
*.tsbuildinfo
.turbo/

# Environment
.env
.env*.local

# Runtime artifacts from mosaic-server: the durable registry database (MOSAIC_DB) and its
# lock. Never a build input — created when the server runs.
*.redb
*.redb.lock

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
