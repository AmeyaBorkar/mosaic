[package]
name = "facet-spin"
version = "0.0.0"
edition = "2021"
publish = false
description = "Adversarial Mosaic Facet: a valid Facet whose `run` never returns."

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
lto = true
strip = true
