[package]
name = "facet-liar"
version = "0.0.0"
edition = "2021"
publish = false
description = "Adversarial Mosaic Facet: a valid Facet whose `alloc` returns a wild pointer."

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
lto = true
strip = true
